#lang racket/base
(provide (all-defined-out))

;; This is here just so Scribble can be happier.

(define big-bang 'mock)
(define stop-when 'mock)
(define on-tick 'mock)
(define on-key 'mock)
(define key=? 'mock)
(define to-draw 'mock)
