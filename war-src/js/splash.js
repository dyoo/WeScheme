// The splash page does very little, but does check to see that the browser is compatible.

goog.require('plt.wescheme.browserCheck');

jQuery(document).ready(function() {
    plt.wescheme.browserCheck();
});
